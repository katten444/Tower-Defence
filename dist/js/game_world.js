const default_types = [
    'default',
    ''
]
const default_game_world_size = {
    x: 7,
    y: 3,
    z: 9
};

function world (type) {
    
    function init_world(type, size){
        if (type == default || null) {
            for 
        }
    }

    function building_slot(type) {
        this.type = type;

    }
}
  

game_world = world()